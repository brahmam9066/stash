import theme from "../../../theme";

const styles = {
    appContainer: {
        flex: 1,
        backgroundColor: "#ffffff"
    },
    errorText: {
        ...theme.linkColor
    }
};

export default styles;
