import theme from "../../../theme";

const styles = {
    appContainer: {
        flex: 1,
        backgroundColor: "#ffffff"
    },
    loginStyle: {
        paddingTop: 26
    },
    errorText: {
        ...theme.linkColor
    }
};

export default styles;
