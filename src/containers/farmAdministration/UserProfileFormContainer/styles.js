import theme from "../../../theme";

const styles = {
    appContainer: {
        flex: 1,
        ...theme.backWhite
    },
    loginFormStyle: {
        paddingTop: 20
    },
    errorText: {
        ...theme.linkColor
    }
};

export default styles;
